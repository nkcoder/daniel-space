import { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  back_of_the_envelope_estimation: 'Back of the Envelope Estimation',
  from_single_server_to_distributed_system: 'From Single Server to Distributed Systems'
};

export default meta;
