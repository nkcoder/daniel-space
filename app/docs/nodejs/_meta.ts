import { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  deploy_nodejs_application: 'Deploy Node.js Application on AWS',
  migrate_from_npm_to_pnpm: 'Migrate from npm to pnpm'
};
export default meta;
