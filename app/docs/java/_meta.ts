import { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  lambda_effective_final: 'Effective Final in Lambdas',
  functional_interface: 'Functional Interface',
  virtual_threads: 'Virtual Threads',
  scoped_value: 'Scoped Value'
};

export default meta;
